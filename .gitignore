# ---------------------------------------------------------------------------
# CONFIDENTIAL SOURCE DATA — never commit.
# These contain client names, colleague data, and raw employer records.
# ---------------------------------------------------------------------------
timesheets/
evals/
data/raw/
resume-source/

# Extraction scripts encode client names and internal codenames in their column
# mappings, so they stay local. They are documented in README.md and run against the
# gitignored source data. privacy_gate.py and sync-content.mjs are safe and committed.
scripts/extract_timesheets.py
scripts/extract_evals.py
scripts/classify_projects.py

# Build output
node_modules/
site/dist/
site/.astro/
dist/

# Generated at build time from content/career.json — never edit or commit
site/src/data/

# Env / secrets
.env
.env.*
.dev.vars

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Resume render output (the shipped PDF is copied into site/public/)
resume/build/
